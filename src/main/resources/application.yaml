spring:
  application:
    name: iteam-api
  datasource:
    url: jdbc:postgresql://localhost:5432/iteam
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081